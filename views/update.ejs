<%- include('partials/header.ejs') %>

<form action="/update" method="post">
    <label for="title">Title</label>
    <input type="hidden" name="id" value="<%= book.id %>">
    <input type="text" name="title" value="<%= book.title %>" id="title" required>
    <label for="author">Author</label>
    <input type="text" name="author" value="<%= book.author %>" id="author" required>
    <label for="isbn">ISBN</label>
    <input type="text" name="isbn" value="<%= book.isbn %>" id="isbn" minlength="1" maxlength="13" required>
    <label for="rating">Rating</label>
    <input type="number" step="0.1" name="rating" value="<%= book.rating %>" id="rating" min="1" max="5" required>
    <label for="notes">Notes</label>
    <textarea name="notes" id="notes" required><%= book.notes %></textarea>
    <% if(locals.error){ %> 
        <p class="err"><%=error%></p>
    <% } %>
    <input class="submit" type="submit" value="Submit">
</form>
<%- include('partials/footer.ejs') %>