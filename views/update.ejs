<%- include('partials/header.ejs') %>

<form action="/update" method="post">
    <label for="title">Title</label>
    <input type="hidden" name="id" value="<%= book.id %>">
    <input type="text" name="title" value="<%= book.title %>" id="title" >
    <label for="author">Author</label>
    <input type="text" name="author" value="<%= book.author %>" id="author" >
    <label for="isbn">ISBN</label>
    <input type="text" name="isbn" value="<%= book.isbn %>" id="isbn" minlength="1" maxlength="13" >
    <label for="rating">Rating</label>
    <input type="number" step="0.1" name="rating" value="<%= book.rating %>" id="rating" min="1" max="5" >
    <label for="notes">Notes</label>
    <textarea name="notes" id="notes" ><%= book.notes %></textarea>
    <% if(locals.error){ %> 
        <p class="err"><%=error%></p>
    <% } %>
    <input class="submit" type="submit" value="Submit">
</form>
<%- include('partials/footer.ejs') %>